{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "_comment": "Copie ce fichier en .claude/settings.local.json (gitignore) et personnalise tes overrides locaux. Ne committe PAS settings.local.json.",
    "permissions": {
        "allow": [
            "Bash(open http://localhost:*)",
            "Bash(php -S localhost:*)"
        ]
    }
}
